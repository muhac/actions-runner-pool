services:
  gharp:
    build: .
    image: gharp:dev
    container_name: gharp
    restart: unless-stopped
    env_file: .env
    ports:
      - "${PORT:-8080}:8080"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - gharp-data:/data

volumes:
  gharp-data:
