name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  lint:
    name: Lint
    runs-on: [self-hosted]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: stable
          cache: true

      - name: golangci-lint
        uses: golangci/golangci-lint-action@v9
        with:
          version: latest

  test:
    name: Build & Test
    runs-on: [self-hosted]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: stable
          cache: true

      - name: go vet
        run: go vet ./...

      - name: go build
        run: go build ./...

      - name: go test
        run: go test -race ./...

  smoke:
    name: Smoke Test
    runs-on: [self-hosted]
    needs: [lint, test]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: stable
          cache: true

      - name: build binary
        run: go build -o gharp ./cmd/gharp

      - name: boot and health check
        run: |
          BASE_URL=http://127.0.0.1:8080 ./gharp &

          for i in $(seq 1 10); do
            sleep $i
            if curl -sf http://localhost:8080/healthz; then
              exit 0
            fi
          done

          echo "health check timed out"
          exit 1
